/* _LWRM_COPYRIGHT_BEGIN_
 *
 * Copyright 2020-2021 by LWPU Corporation.  All rights reserved.  All
 * information contained herein is proprietary and confidential to LWPU
 * Corporation.  Any use, reproduction, or disclosure without the written
 * permission of LWPU Corporation is prohibited.
 *
 * _LWRM_COPYRIGHT_END_
 */

/*
 * This is a fake lwoc header which includes ucode access functions. 
 * This file is only used for generating halinfo to FlcnMcheck.
 */

#include "g_chips2halspec.h"        // generated by rmconfig.pl

/*! Macro to help specify prefixes on LWOC classes */
#define LWOC_PREFIX(x) [[lwoc::prefix(x)]]

/*! fake BINDATA_ARCHIVE type */
typedef struct {} BINDATA_ARCHIVE;

/*! fake BINDATA_STORAGE type */
typedef struct BINDATA_STORAGE *PBINDATA_STORAGE;

typedef LwU32 LW_STATUS;

#define NULL                    0
#define LW_ERR_NOT_SUPPORTED    0
#define STUB_RETURN(v)          { return v; }
#define RMCFG_IS_PLATFORM(P)    (RMCFG_FEATURE_PLATFORM_##P)

typedef struct OBJGPU OBJGPU;
typedef struct OBJGPU *POBJGPU;
typedef class OBJDUMMY OBJACR, *POBJDISP;
typedef class OBJDUMMY *POBJACR;
typedef class OBJDUMMY Pmu;
typedef class OBJDUMMY OBJBSP;
typedef class OBJDUMMY OBJSEC2, *POBJSEC2;

LWOC_PREFIX(dummy) class OBJDUMMY
{
    ChipHal     chipHal;
    DispIpHal   dispIpHal;

#ifdef FLCN_TARGET_DPU
#include "gpu/disp/objdisp_bin_hal.h"
#endif // FLCN_TARGET_DPU

#ifdef FLCN_TARGET_ACR
#include "objacr_bin_hal.h"
#endif // FLCN_TARGET_ACR

#ifdef FLCN_TARGET_PMU
#include "gpu/pmu/pmu_bin_data_obj.h"
#endif // FLCN_TARGET_PMU

#if defined(FLCN_TARGET_FUB) || defined(FLCN_TARGET_SELWRESCRUB)
#include "objbsp_bin_hal.h"
#endif // FLCN_TARGET_FUB || FLCN_TARGET_SELWRESCRUB

#ifdef FLCN_TARGET_SEC2
#include "sec2/sec2_bin_data_obj.h"
#endif // FLCN_TARGET_SEC2

};
