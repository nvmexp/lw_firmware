# -*- mode: perl; -*-

# HWV Hal interface definitions
# For a brief introduction to file format, see README in this directory.

my $hwvHal = [

    DEFAULTS => [
        GCC_ATTRIBUTES  => 'GCC_ATTRIB_SECTION("imem_hwv", "__FUNC__")',
        FLAGS           => ':NO_HAL'
    ],

    # HAL Interfaces
    # **************************************************************************

    HAL_INTERFACES => [

    ],   # HAL_INTERFACES
];

# return a reference to the Hal definition
return $hwvHal;

