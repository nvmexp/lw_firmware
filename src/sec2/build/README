sec2/build

scripts, HS signing config, and related files for SEC2 ucode build

$LW_ROOT/uproc/sec2/build/sign     -- HS signature configuration files for each profile
                                   -- siggen uses these files to sign HS overlays

For other details, refer to:

    $LW_ROOT/pmu_sw/build/README
    ../../../pmu_sw/build/README
