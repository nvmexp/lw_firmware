# -*- mode: perl; -*-

# LOGGER Chip Hal interface definitions
# For a brief introduction to file format, see README in this directory.

my $loggerHal = [

    DEFAULTS => [
        FLAGS => ':NO_HAL'
    ],

    # HAL Interfaces
    # **************************************************************************

    HAL_INTERFACES => [
    ],   # HAL_INTERFACES

];

# return a reference to the Hal definition
return $loggerHal;

