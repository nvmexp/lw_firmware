pmu_sw/build

scripts and related files for PMU ucode build

    Makefile                -- Makefile to run mcheck in DVS

    mcheck/...              -- The mcheck script.  Check wiki for details.
        mcheck.pl           -- The entry script to run mcheck test
        mcheck.config       -- The config file of mcheck test

    OverlaysImem.pm         -- config file for PMU IMEM Overlays
                               used to generate linker scripts
                               (for Falcon profiles)

    OverlaysDmem.pm         -- config file for PMU DMEM Overlays
                               used to generate linker scripts
                               (for Falcon profiles)

    RiscvSectionsCode.pm    -- config file for PMU code sections
                               used to generate linker scripts
                               (for RISCV profiles)

    RiscvSectionsData.pm    -- config file for PMU data sections
                               used to generate linker scripts
                               (for RISCV profiles)

    Tasks.pm                -- config file for OS Tasks

    Profiles.pm             -- config file for PMU ucode builds

    pmu-script.pl           -- wrapper script to call rtos-flcn-script.pl

    genRmProp.pl            -- entry script calls genRmProp.pm to generate
                               RM Properties based on profile features

    vcast_preprocess.pl     -- pre-process C files passed into script to
                               skip instrumenting error handling code blocks
