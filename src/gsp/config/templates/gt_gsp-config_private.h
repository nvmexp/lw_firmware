// This file is automatically generated by chip-config - DO NOT EDIT!
//
// Profile:  {{$PROFILE}}
// Template: {{$TEMPLATE_FILE}}
//
// Chips:    {{ CHIP_LIST() }}
//

#ifndef G_{{$XXCFG}}_PRIVATE_H
#define G_{{$XXCFG}}_PRIVATE_H

//
// Macros to help with enabling or disabling code based on whether
// a feature (or chip or engine or ...) is enabled or not.
// Also have GSPCFG_CHIP_ENABLED(), GSPCFG_FEATURE_ENABLED(), etc
// from {{$XXCONFIG_H_FILE}}.
//
// NOTE: these definitions are "flat" (ie they don't use some more general
//       GSPCFG_ENABLED(CHIP,X) form because the pre-processor would re-evaluate
//       the expansion of the item (gpu, feature, class, api).  For classes,
//       at least, this is a problem since we would end up with class number
//       instead of its name...

// hack: MSVC is not C99 compliant
#ifdef LW_WINDOWS
#define __func__ __FUNCTION__
#endif

// CHIP's
#define GSPCFG_CHIP_ENABLED_STATUS(W)   (GSPCFG_CHIP_##W ? {{$STATUS_OK}} : {{$STATUS_ERROR}})
#define GSPCFG_CHIP_ENABLED_OR_BAIL(W)                \
     do {                                           \
         if ( ! GSPCFG_CHIP_##W)                      \
         {                                          \
             DBG_PRINTF((DBG_MODULE_GLOBAL, DBG_LEVEL_ERRORS, "GSP: %s: CHIP " #W " not enabled, bailing\n", __func__)); \
             return {{$STATUS_ERROR}};           \
         }                                          \
     } while (LW_FALSE)
#define GSPCFG_CHIP_ENABLED_OR_ASSERT_AND_BAIL(W)     \
     do {                                           \
         if ( ! GSPCFG_CHIP_##W)                      \
         {                                          \
             DBG_PRINTF((DBG_MODULE_GLOBAL, DBG_LEVEL_ERRORS, "GSP: %s: CHIP " #W " not enabled, assert and bail\n", __func__)); \
             {{$ASSERT}}(GSPCFG_CHIP_##W);                        \
             return {{$STATUS_ERROR}};           \
         }                                          \
     } while (LW_FALSE)
#define GSPCFG_CHIP_ENABLED_OR_GOTO(W,L)  \
    do {                                 \
        if (! GSPCFG_CHIP_##W)            \
        {                                \
            goto L;                      \
        }                                \
    } while (LW_FALSE)
#define GSPCFG_CHIP_ENABLED_OR_BREAK(W)   \
    {                                    \
        if (! GSPCFG_CHIP_##W)            \
        {                                \
            break;                       \
        }                                \
    }

// FEATURE's
#define GSPCFG_FEATURE_ENABLED_STATUS(W)   (GSPCFG_FEATURE_##W ? {{$STATUS_OK}} : {{$STATUS_ERROR}})
#define GSPCFG_FEATURE_ENABLED_OR_BAIL(W)            \
     do {                                           \
         if ( ! GSPCFG_FEATURE_##W)                  \
         {                                          \
             DBG_PRINTF((DBG_MODULE_GLOBAL, DBG_LEVEL_ERRORS, "GSP: %s: FEATURE " #W " not enabled, bailing\n", __func__)); \
             return {{$STATUS_ERROR}};           \
         }                                          \
     } while (LW_FALSE)
#define GSPCFG_FEATURE_ENABLED_OR_ASSERT_AND_BAIL(W) \
     do {                                           \
         if ( ! GSPCFG_FEATURE_##W)                  \
         {                                          \
             DBG_PRINTF((DBG_MODULE_GLOBAL, DBG_LEVEL_ERRORS, "GSP: %s: FEATURE " #W " not enabled, assert and bail\n", __func__)); \
             {{$ASSERT}}(GSPCFG_FEATURE_##W);          \
             return {{$STATUS_ERROR}};           \
         }                                          \
     } while (LW_FALSE)
#define GSPCFG_FEATURE_ENABLED_OR_GOTO(W,L)  \
    do {                                     \
        if (! GSPCFG_FEATURE_##W)            \
        {                                    \
            goto L;                          \
        }                                    \
    } while (LW_FALSE)
#define GSPCFG_FEATURE_ENABLED_OR_BREAK(W)   \
    {                                        \
        if (! GSPCFG_FEATURE_##W)            \
        {                                    \
            break;                           \
        }                                    \
    }

// ENGINE's
#define GSPCFG_ENGINE_ENABLED_STATUS(W)   (GSPCFG_ENGINE_##W ? {{$STATUS_OK}} : {{$STATUS_ERROR}})
#define GSPCFG_ENGINE_ENABLED_OR_BAIL(W)             \
     do {                                           \
         if ( ! GSPCFG_ENGINE_##W)                   \
         {                                          \
             DBG_PRINTF((DBG_MODULE_GLOBAL, DBG_LEVEL_ERRORS, "GSP: %s: ENGINE " #W " not enabled, bailing\n", __func__)); \
             return {{$STATUS_ERROR}};           \
         }                                          \
     } while (LW_FALSE)
#define GSPCFG_ENGINE_ENABLED_OR_ASSERT_AND_BAIL(W)  \
     do {                                           \
         if ( ! GSPCFG_ENGINE_##W)                   \
         {                                          \
             DBG_PRINTF((DBG_MODULE_GLOBAL, DBG_LEVEL_ERRORS, "GSP: %s: ENGINE " #W " not enabled, assert and bail\n", __func__)); \
             {{$ASSERT}}(GSPCFG_ENGINE_##W);           \
             return {{$STATUS_ERROR}};           \
         }                                          \
     } while (LW_FALSE)
#define GSPCFG_ENGINE_ENABLED_OR_GOTO(W,L)  \
    do {                                    \
        if (! GSPCFG_ENGINE_##W)            \
        {                                   \
            goto L;                         \
        }                                   \
    } while (LW_FALSE)
#define GSPCFG_ENGINE_ENABLED_OR_BREAK(W)   \
    {                                       \
        if (! GSPCFG_ENGINE_##W)            \
        {                                   \
            break;                          \
        }                                   \
    }


#endif  // G_{{$XXCFG}}_PRIVATE_H
